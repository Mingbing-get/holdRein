## 待修改任务

1. 现在的shell命令是设置了timeout的，但是很多命令就是需要执行很长的时间或者就是需要一直运行着，是否可以改成这个timeout只是一个监听的最长时间，若这个时间过了还没有执行完不会杀掉进程，只是完成了这个tool，并标识命令还在运行中，同时实现一个全局的shell管理工具，对于这种超时还未执行完成的命令就注册到这个工具中去；再加一个可以读取之前shell结果的工具，用于大模型后续查看shell，和一个杀掉之前shell命令的工具，用于大模型可以自主结束长时间的命令(这里有一个疑问，就是如何来管理这些命令的生命周期，是任务结束就结束所有的任务，任务结束就从全局的管理工具中移除命令吗？如果移除了后续前端是否就无法再看到曾经注册过的命令了，是否有必要长期保留)；前端部分实现一个右侧面板用于查看当前任务注册过哪些长时命令，以及这些命令的状态，并且支持手动停止正在运行中的命令
2. api端启动的时候需要去数据库中查询是否还有被标记为运行中的任务或者子任务，若有则应该将其标记为中断（可能是正在执行中但是停止了服务造成的，其本质就是任务中断了）
3. 需要实现skill安装、管理，支持从github安装（都是直接安装到 home/.hold-rein/skills下），在skills下需要有一个json配置文件(服务启动的时候就会加载一下这个文件，后续若有修改会同时修改内存中的信息和这个配置文件)，用于标记技能是否禁用（只要没有在这里标记为禁用就是启用的），若用户手动设置了某个技能禁用则会在这里存下来，在调用模型的时候会将禁用的skill排除掉；前端部分在设置界面实现一个技能管理页面，在模型管理之后加一个技能管理的nav，点击后打开技能管理的主界面（在module下新建一个文件夹来实现）,将所有安装的技能以卡片的形式展示出来，每个卡片有一个禁用/启用的switch切换按钮和一个卸载的按钮，整个板块右上角有一个安装技能的按钮，点击后弹出一个弹窗，用户只需要输入github的仓库地址点击安装即可成功安装技能

## 待考虑任务

1. 文件长期记忆系统，提供search_memory、read_memory，默认嵌入记忆文件夹的结构，任务完成后重新启动一个agent来整理记忆提供write_memory、update_memory、search_memory、read_memory、list_memory_structure等工具
