## 待修改任务

1. 用量统计的折线图，现在顶部流出来显示图例的空间是固定的，但是有可能使用的模型太多会导致这里盖住了折线图，是否可以改成动态适配
2. 现在的shell命令是设置了timeout的，但是很多命令就是需要执行很长的时间或者就是需要一直运行着，是否可以实现一个全局的shell管理工具；然后调用工具改成监听一段时间，若这段时间内完成了就直接返回，若没有完成则先获取已经有的输出，并提示还在运行；再加一个可以可以读取之前shell结果的工具，用于大模型后续查看shell，同时实现一个右侧面板用于查看当前任务运行过哪些命令，以及这些命令的状态，并且支持手动停止正在运行中的命令；这些命令即使任务结束都还是在运行着的，但是如果任务结束了并且命令也执行结束了就需要清理掉这个命令的记录（或者实现一个永久存储，将执行的命令都存储起来）
3. api端启动的时候需要去数据库中查询是否还有被标记为运行中的任务或者子任务，若有则应该将其标记为中断（可能是正在执行中但是停止了服务造成的，其本质就是任务中断了）
4. 需要实现skill安装、管理，支持从github安装（都是直接安装到 home/.hold-rein/skills下），在skills下需要有一个json配置文件(服务启动的时候就会加载一下这个文件，后续若有修改会同时修改内存中的信息和这个配置文件)，用于标记技能是否禁用（只要没有在这里标记为禁用就是启用的），若用户手动设置了某个技能禁用则会在这里存下来，在调用模型的时候会将禁用的skill排除掉；前端部分在设置界面实现一个技能管理页面，在模型管理之后加一个技能管理的nav，点击后打开技能管理的主界面（在module下新建一个文件夹来实现）,将所有安装的技能以卡片的形式展示出来，每个卡片有一个禁用/启用的switch切换按钮和一个卸载的按钮，整个板块右上角有一个安装技能的按钮，点击后弹出一个弹窗，用户只需要输入github的仓库地址点击安装即可成功安装技能

## 待考虑任务

1. 文件长期记忆系统，提供search_memory、read_memory，默认嵌入记忆文件夹的结构，任务完成后重新启动一个agent来整理记忆提供write_memory、update_memory、search_memory、read_memory、list_memory_structure等工具
